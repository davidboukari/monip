# monip

```
/sbin/ip -4 addr

/sbin/ip -6 addr


* IPV4 publique:

curl ifconfig.me

* IPV6 publique (avec filtrage pour n'afficher que l'ip):

curl -s http://checkipv6.dyndns.org | cut -c77-111
```
